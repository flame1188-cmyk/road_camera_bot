"""
Тонкая обёртка для обратной совместимости.

Весь код бота теперь находится в пакете `bot/` (см. bot/__init__.py).
Этот файл сохранён, чтобы:
  1. Команда `python bot.py` продолжала работать как раньше.
  2. Тесты, импортирующие `from bot import X`, разрешались через пакет.

Phase 3-2: 100% pure refactoring — никакая логика не изменена, только
перемещена в модули.
"""
from bot.app import main

if __name__ == "__main__":
    main()
